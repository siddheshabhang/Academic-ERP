# ==========================
# H2 DATABASE CONFIGURATION
# ==========================
spring.datasource.url=jdbc:mysql://localhost:3306/academic_erp?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=1908siddhesh
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================
# JPA CONFIG (use manual schema SQL)
# ==========================
spring.sql.init.mode=always
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ==========================
# H2 CONSOLE
# ==========================
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ==========================
# SWAGGER / OPENAPI
# ==========================
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# ==========================
# JWT SETTINGS
# ==========================
app.jwt.secret=MySuperSecretKeyForJWTPlacementProject123!
app.jwt.expiration=86400000